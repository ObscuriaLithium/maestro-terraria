plugins {
    id 'base'
}

tasks.register('buildPack', Zip) {
    archiveFileName = "releases/Maestro-Terraria.zip"
    destinationDirectory = projectDir

    from(projectDir) {
        include 'assets/**'
        include 'pack.mcmeta'
        include 'pack.png'
    }
}